[package]
name = "importer-import"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
importer-manifest = { path = "../importer-manifest" }
tempfile.workspace = true
thiserror.workspace = true

[lints]
workspace = true

[dev-dependencies]
importer-domain = { path = "../importer-domain" }
importer-media = { path = "../importer-media" }
importer-plan = { path = "../importer-plan" }
